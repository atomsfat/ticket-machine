tags:
  - admin
  - mock
operationId: deleteVenue
summary: Delete a specific venue
consumes:
  - applicaton/json
produces:
  - application/json
parameters:
  - in: path
    name: venueID
    description: Venue id
    required: true
    type: string
responses:
  404:
    description: Delete especific venue
    schema:
      $ref: '#/definitions/Message'

