tags:
  - admin
  - mock
operationId: putVenue
summary: Update venue
consumes:
  - applicaton/json
produces:
  - application/json
parameters:
  - in: body
    name: Venue
    schema:
      $ref: '#/definitions/Venue'
responses:
  200:
    description: Create an Venue
    schema:
      $ref: '#/definitions/Venue'

